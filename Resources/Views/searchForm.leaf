<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>🌝 Поиск 🌝</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    </head>
    <body>
    
        <form method="GET" action="/searchResultView?key=#(key)&value=#(language)">
            <div class="block" style="margin-left: 32%; margin-right: 32%; margin-top: 5%">
                <div class="field">
                  <label class="label">Слово для поиска</label>
                  <div class="control">
                    <input class="input" type="text" name="key" value="#(key)" placeholder="Введите слово (опционально)">
                  </div>
                </div>
                
                <div class="field">
                  <label class="label">Его язык</label>
                  <div class="control">
                    <input class="input" type="text" name="language" value="#(language)" placeholder="Введите язык (опционально)">
                  </div>
                </div>
                
                <div class="control">
                  <button type="submit" class="button is-primary">Найти</button>
                </div>
            </div>
        </form>
        
    </body>
</html>
